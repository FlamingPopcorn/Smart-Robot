// ---------------------------------------------------------------- //
// This file is autogenerated by pioasm version 2.2.0; do not edit! //
// ---------------------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// --------------- //
// pollReflectance //
// --------------- //

#define pollReflectance_wrap_target 1
#define pollReflectance_wrap 16
#define pollReflectance_pio_version 1

#define pollReflectance_TIMEOUT_LOOPS 45000

static const uint16_t pollReflectance_program_instructions[] = {
    0x80a0, //  0: pull   block
            //     .wrap_target
    0xe081, //  1: set    pindirs, 1
    0xe001, //  2: set    pins, 1
    0xe03d, //  3: set    x, 29
    0xbd42, //  4: nop                           [29]
    0xbd42, //  5: nop                           [29]
    0xbd42, //  6: nop                           [29]
    0xbd42, //  7: nop                           [29]
    0xbd42, //  8: nop                           [29]
    0x1d44, //  9: jmp    x--, 4                 [29]
    0xe080, // 10: set    pindirs, 0
    0xa027, // 11: mov    x, osr
    0x00ce, // 12: jmp    pin, 14
    0x000f, // 13: jmp    15
    0x004c, // 14: jmp    x--, 12
    0xa0c1, // 15: mov    isr, x
    0x8000, // 16: push   noblock
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program pollReflectance_program = {
    .instructions = pollReflectance_program_instructions,
    .length = 17,
    .origin = -1,
    .pio_version = pollReflectance_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config pollReflectance_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + pollReflectance_wrap_target, offset + pollReflectance_wrap);
    return c;
}

static inline void pollReflectance_program_init(PIO pio, uint sm, uint offset, uint pin) {
    ; // gpio_init(pin);
    ; // gpio_set_dir(pin, GPIO_OUT);
    ; // gpio_put(pin, 1);
    ; // sleep_us(10); // Hold it high briefly
    pio_sm_config c = pollReflectance_program_get_default_config(offset);     // Get the default config for (clk_div, ISR, OSR, FIFOs, Pin mappings, etc)
    ; // sm_config_set_clkdiv(&c, 1.0f);
    sm_config_set_set_pins(&c, pin, 1);             // Configures the set pin
    sm_config_set_in_pins(&c, pin);
    sm_config_set_jmp_pin(&c, pin);                 // Configures the jmp pin
    ; // sm_config_set_fi0fo_join(&c, PIO_FIFO_JOIN_RX);  // Configures the TX FIFO to join the RX FIFO for 8 slots total
    ; // gpio_set_function(pin, GPIO_FUNC_PIO1);    
    pio_gpio_init(pio, pin);                        // Connect the GPIO to the PIO block
    ; // gpio_set_input_enabled(pin, true);
    gpio_disable_pulls(pin);
    pio_sm_init(pio, sm, offset, &c);               // Intitialize the State Machine
    pio_sm_set_enabled(pio, sm, true);              // Enable the State Machine
}

#endif

