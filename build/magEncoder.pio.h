// ---------------------------------------------------------------- //
// This file is autogenerated by pioasm version 2.2.0; do not edit! //
// ---------------------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// ---------- //
// magEncoder //
// ---------- //

#define magEncoder_wrap_target 1
#define magEncoder_wrap 6
#define magEncoder_pio_version 1

static const uint16_t magEncoder_program_instructions[] = {
    0xa04b, //  0: mov    y, ~null
            //     .wrap_target
    0x20a0, //  1: wait   1 pin, 0
    0xa02b, //  2: mov    x, ~null
    0x00c6, //  3: jmp    pin, 6
    0xa041, //  4: mov    y, x
    0x0001, //  5: jmp    1
    0x0043, //  6: jmp    x--, 3
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program magEncoder_program = {
    .instructions = magEncoder_program_instructions,
    .length = 7,
    .origin = -1,
    .pio_version = magEncoder_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config magEncoder_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + magEncoder_wrap_target, offset + magEncoder_wrap);
    return c;
}

static inline void magEncoder_program_init(PIO pio, uint sm, uint offset, uint pin) {
    pio_sm_config c = magEncoder_program_get_default_config(offset);     // Get the default config for (clk_div, ISR, OSR, FIFOs, Pin mappings, etc)
    sm_config_set_in_pins(&c, pin);         // Configures the wait pin
    sm_config_set_jmp_pin(&c, pin);         // Configures the jmp pin
    pio_gpio_init(pio, pin);                // Connect the GPIO to the PIO block
    pio_sm_init(pio, sm, offset, &c);       // Intitialize the State Machine
    pio_sm_set_enabled(pio, sm, true);      // Enable the State Machine
}

#endif

